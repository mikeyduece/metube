<div class='container'>
  <div class='row'>
    <div class='col-md-1'>
      <% @user.playlists.each do |playlist| %>
        <%= link_to playlist.name, playlist_path(playlist.id), class: 'play_list' %>
      <% end %>
    </div>
  </div>
</div>
